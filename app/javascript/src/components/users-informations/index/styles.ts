import { makeStyles } from '@material-ui/core/styles';

export const useStyles = makeStyles({
  container: {
    minHeight: '100%',
    maxHeight: '100%',
  },
  title: {
    textAlign: 'center',
    padding: '20px 0',
    fontSize: '35px',
  },
  footer: {
    height: '70px'
  },
});